# Azure OpenAI (CopilotKit chat popup).
# Key from Azure Portal → Cognitive Services resource → Keys and Endpoint
OPENAI_API_KEY=

# Deployment name in Azure OpenAI Studio (e.g. gpt-4o, gpt-4)
AZURE_OPENAI_DEPLOYMENT=gpt-4o
