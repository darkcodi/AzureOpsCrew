@inherits LayoutComponentBase

<div class="discord-layout">
    @if (ServerStrip != null)
    {
        @ServerStrip
    }
    @if (LeftSidebar != null)
    {
        @LeftSidebar
    }
    @if (MainArea != null)
    {
        @MainArea
    }
    @if (RightSidebar != null)
    {
        @RightSidebar
    }
</div>

@code {
    [Parameter] public RenderFragment? ServerStrip { get; set; }
    [Parameter] public RenderFragment? LeftSidebar { get; set; }
    [Parameter] public RenderFragment? MainArea { get; set; }
    [Parameter] public RenderFragment? RightSidebar { get; set; }
}
