@page "/"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect to the default server page
        Navigation.NavigateTo("/server", forceLoad: true);
    }
}
