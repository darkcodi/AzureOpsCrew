@page "/"
@inject NavigationManager Navigation

@code {
    private readonly Guid _defaultServerId = Guid.Empty;

    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"/server/{_defaultServerId}", forceLoad: true);
    }
}
